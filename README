This folder contains:
quickSelect.py: creates an array of random size with random variables and selects the predetermined 
kth smallest variable within the set using the quick select algorithm.
binarySearch.py: creates a randomly sized and specified 2D array in ascending order that can be searched
for a specific variable which gets returned using the binary search algorithm.

To Compile and Run:
chmod +x makefile
./makefile
./quickselect.py
./binarysearch.py

OR

python3 quickselect.py
python3 binarysearch.py
